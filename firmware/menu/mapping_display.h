#pragma once

// Mapping Display - Split-screen visualization of input/output controller mapping
// Shows input controller on left half, output controller on right half.

bool isMappingDisplayActive();
void activateMappingDisplay();
void deactivateMappingDisplay();
bool renderMappingDisplay();
